import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Financial Telegram Member Login | FinTelegram News",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
