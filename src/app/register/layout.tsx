import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register and Subscribe for Financial Telegram | FinTelegram News",
};

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
